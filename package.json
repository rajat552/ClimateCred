{
  "name": "climatetwin",
  "private": true,
  "version": "0.1.0",
  "description": "ClimateTwin — MSME Climate-Risk & Green Viability Intelligence Engine",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "clean": "turbo run clean",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@climatetwin/eslint-config": "workspace:*",
    "@climatetwin/typescript-config": "workspace:*",
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.11.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
